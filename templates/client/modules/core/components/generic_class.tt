import React, { PropTypes } from 'react';<% if (childrenImports) { %><%= childrenImports %><% } %>

class <%= componentName %> extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <%= componentName %><% if (childrenComponents)  { %><%= childrenComponents %><% } %>
      </div>
    );
  }
}
<%= componentName %>.propTypes = {};

export default <%= componentName %>;
