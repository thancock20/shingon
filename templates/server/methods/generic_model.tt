import {<%= collectionName %>} from '/lib/collections';
import defaultMethods from '/lib/default_methods';

export default function () {
  defaultMethods('<%= methodFileName %>', <%= collectionName %>);
}
